# KIS API & HTTP
requests>=2.31.0
websockets>=12.0
aiohttp>=3.11.0

# 데이터 처리
numpy>=2.2.6
pandas>=2.3.2

# 기술적 분석: RSI/MACD/볼린저/이평선은 pandas .ewm()/.rolling()로 직접 구현
# (src/analysis/technical_indicators.py) — pandas-ta 등 별도 TA 라이브러리 불필요.
# 예전에 pandas-ta가 있었으나 실제로는 어디서도 import 안 돼 numba/llvmlite만
# 매 실행(10분 간격)마다 불필요하게 설치되고 있어 제거 (2026-08-24)

# Slack
slack-bolt>=1.18.0
slack-sdk>=3.27.0

# 스케줄러
APScheduler>=3.10.0

# 설정 파일
PyYAML>=6.0.2
python-dotenv>=1.0.0

# 로깅
colorlog>=6.8.0

# 유틸
python-dateutil>=2.9.0
pytz>=2024.1

# Supabase
supabase>=2.0.0

# 한국거래소 시세 데이터 (일봉, API 키 불필요)
finance-datareader>=0.9.50

# 한국 공휴일·대체공휴일 판단
holidays>=0.46

# KRX 공매도 비중 조회 (2026-08-28 추가) — src/api/krx_data.py
# ⚠️ KRX 회원 로그인(KRX_ID/KRX_PW 환경변수) 필요 — 최초엔 "API 키 불필요"로 잘못 적었다가
# 실측(드라이런)으로 정정, 미설정 시 이 기능만 조용히 비활성화(스캔엔 영향 없음). 상세는 krx_data.py 참고
pykrx>=1.0.51

# 통계적 유의성 검정 (backtest_trading_rules.py sig_test() — t-검정)
# 2026-09-14: GitHub Actions clean venv에 여태 없었던 게 이번에 backtest_trading_rules.yml
# 워크플로우를 처음 추가해 실제로 돌려보고서야 드러남 — 그동안 t-검정 결과가 전부
# "검정 실패(No module named 'scipy')"로 조용히 누락되고 있었을 가능성 있음
scipy>=1.11.0

# 기술점수 ML 대체 실험 (backtest_ml_technical_score.py, 2026-09-17 추가)
# Ridge/LogisticRegression만 사용 — 신호 자체가 약해(corr≤0.03) 복잡한 비선형 모델은
# 노이즈 과적합 위험이 커서 1차 실험에서 의도적으로 배제, 위 CLAUDE.md 참고
scikit-learn>=1.4.0
